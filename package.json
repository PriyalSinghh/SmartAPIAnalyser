{
  "name": "smart-api-analyzer",
  "version": "2.0.0",
  "private": true,
  "description": "SmartAPI - Production-Scale API Failure Analyzer Fullstack Application",
  "scripts": {
    "build": "cd frontend && npm install && npm run build",
    "start": "cd backend && npm start",
    "test": "cd backend && npm test",
    "db:init": "cd backend && npm run db:init",
    "db:migrate": "cd backend && npm run db:migrate",
    "seed": "cd backend && npm run seed",
    "seed:large": "cd backend && npm run seed:large"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.4.7",
    "express": "^4.21.2",
    "express-rate-limit": "^7.5.0",
    "ioredis": "^5.4.2",
    "morgan": "^1.10.0",
    "pg": "^8.13.1"
  }
}
